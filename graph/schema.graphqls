type Post {
    id: ID!
}

type Comment {
    id: ID!
    postId: ID!
    content: String!
}

type Query {
    comments: [Comment]!
}

type Mutation {
    createPost:Post!
    createComment(input: CreateCommentInput!): Comment!
}

type Subscription {
    commentAdded:Comment!
}

input CreateCommentInput{
    postId: ID!
    content: String!
}